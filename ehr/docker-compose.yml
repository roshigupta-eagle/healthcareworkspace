version: '3.8'
services:
  ehr:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - EHR_DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
