"use client";

import React from 'react';
import WidgetEmptyState from '../WidgetEmptyState';

export default function AllQueuesTab() {
  return <WidgetEmptyState title="All queues" message="Team and clinic-wide queues will appear here when available." />;
}
